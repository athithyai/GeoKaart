services:
  app:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - cbs_data:/app/backend/data   # persist DuckDB files across restarts
    env_file:
      - .env
    restart: unless-stopped

volumes:
  cbs_data:
